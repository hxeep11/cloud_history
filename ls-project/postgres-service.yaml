apiVersion: v1
kind: Service
metadata:
  name: ls-project-postgres
  namespace: ls-blogging-system
  labels:
    app: ls-project-postgres
spec:
  type: ClusterIP
  selector:
    app: ls-project-postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
