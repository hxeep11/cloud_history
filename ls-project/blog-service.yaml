apiVersion: v1
kind: Service
metadata:
  name: blog-service
  namespace: blogging-system
  labels:
    app: blog-service
spec:
  type: ClusterIP
  selector:
    app: blog-service
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
