apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: ls-project-postgres
  namespace: ls-blogging-system
  labels:
    app: ls-project-postgres
spec:
  serviceName: ls-project-postgres
  replicas: 1
  selector:
    matchLabels:
      app: ls-project-postgres
  template:
    metadata:
      labels:
        app: ls-project-postgres
    spec:
      containers:
        - name: postgres
          image: postgres:15-alpine
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_DB
              value: sl_project_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: username
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: db-credentials
                  key: password
          volumeMounts:
            - name: pgdata
              mountPath: /var/lib/postgresql/data
              subPath: data
  volumeClaimTemplates:
    - metadata:
        name: pgdata
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
