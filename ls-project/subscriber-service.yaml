apiVersion: v1
kind: Service
metadata:
  name: subscriber-service
  namespace: blogging-system
  labels:
    app: subscriber-service
spec:
  type: ClusterIP
  selector:
    app: subscriber-service
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
