apiVersion: v1
kind: Service
metadata:
  name: subscriber-service          # 또는 subscribe-service
  namespace: ls-blogging-system
  labels:
    app: subscriber-service
spec:
  type: ClusterIP
  selector:
    app: subscriber-service
  ports:
    - port: 80
      targetPort: 80               # ✅ nginx 컨테이너 포트랑 맞춤
      protocol: TCP
